<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    tool:layout_height="wrap_content"
    tool:layout_width="wrap_content">

    <ImageView
        android:id="@+id/iv_product_bg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_mypage_product_bg"
        android:backgroundTint="@color/white"
        android:padding="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_product_description_bg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_mypage_product_description_bg"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_product_bg" />

    <TextView
        android:id="@+id/tv_product_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/brown_grey"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/iv_product_description_bg"
        app:layout_constraintEnd_toEndOf="@id/iv_product_description_bg"
        app:layout_constraintStart_toStartOf="@id/iv_product_description_bg"
        app:layout_constraintTop_toTopOf="@id/iv_product_description_bg"
        tool:text="로보킬" />

    <ImageView
        android:id="@+id/iv_heart"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_marginVertical="4dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_heart_fill"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_num_of_interests"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/brown_grey"
        android:textSize="11sp"
        app:layout_constraintEnd_toEndOf="@id/iv_heart"
        app:layout_constraintStart_toStartOf="@id/iv_heart"
        app:layout_constraintTop_toBottomOf="@id/iv_heart"
        tool:text="200" />

</androidx.constraintlayout.widget.ConstraintLayout>